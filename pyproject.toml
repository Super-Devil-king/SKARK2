[tool.poetry]
name = "Skark-website"
version = "0.1.0"
description = "on it"
authors = ["Ranbir"]

[tool.poetry.dependencies]
python = "^3.11"
Flask = "^2.2.3"
requests = "^2.28.1"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
