[tool.poetry]
name = "skark-website"  # Should match the package name
version = "0.1.0"
description = "A short description of your project"
authors = ["Your Name <you@example.com>"]

[tool.poetry.dependencies]
python = "^3.11"
Flask = "^2.3.3"
requests = "^2.32.3"

[tool.poetry.packages]
include = [{ include = "skark_website", from = "." }]  # This line ensures Poetry knows where to find your package

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
